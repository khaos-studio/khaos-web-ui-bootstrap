{
  "name": "khaos-web-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.4.1",
  "workspaces": [
    "windows/*",
    "shared/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "@pinia/nuxt": "^0.5.0",
    "prettier": "^3.0.0",
    "turbo": "^2.0.0"
  }
}
