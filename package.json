{
  "name": "khaos-web-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "windows/*",
    "shared/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
